// Build tag shown in the page footer (and anywhere else that wants it).
// Bump the rN with every design change — it's how playtests confirm which
// build the browser actually loaded (Safari caches ES modules aggressively).
export const BUILD = 'r74';
