# Booth session 2 — 2026-08-30, build r20 (enemy identity art pass)

Jacob on the stick (flag moved to button 3 after button 7 turned out to be his
focus). Five findings, all actioned same-session; replies flowed through the
panel (reopen-on-badge bug found and fixed mid-session).

## Flags and outcomes

1. **"The new shapes are good — but the zako's nose doesn't point where it
   flies."** (S1, run 3.) Renderer bug: heading used stored vx while the core
   adds a ±0.6px/f sine wobble larger than vx (0.47). Fixed: the sprite heading
   includes the same wobble term.
2. **"The midboss floats down centre then teleports to the left — since
   day 1."** (S4, run 4.) Real core bug: the rail sweep ran off `e.age` (~89 at
   park), so the first parked frame snapped centre → rail. Fixed as a 60-frame
   glide onto the unchanged sweep path. Referee vs r19 control: s4_dynamic
   green (1.82), s6_alignment 2.81 vs bar 3 RED (recert class — the bots'
   point-blank timing on the midboss moved), s7_robust facade boss-p3 →
   midboss. Kept: a visible teleport is a defect; the bar encodes the old path.
3. **"Stage 1 music doesn't stop under the WARNING."** Confirmed by one probe
   question (both tracks audible): the duck was a 0.9s fade, still audible
   under a 1.2s siren. Fixed: hard cut (0.12s), boss track starts clean.
4. **"The purple triangle reads as the hitbox but the real one is at its tip —
   you get hit when it feels like you should not have."** Chain of fixes across
   the session, each pushed further by Jacob: core dot moved to the true spot
   and always drawn → "now the dot grows on focus and that's not the hitbox
   either" → genre research (Touhou 10×10 marker over 3.3–7px truth; markers
   never smaller than truth) → ship dot drawn at the 6px effective radius,
   ship sprite 18 → 28px, focus = shape not colour.
5. **"Isn't the bullet doing the same thing?" + marked-up screenshot proving
   it** (red truth circle outside the white core). Conceded and fixed: bullet
   white cores drawn at their true 3px; "white = what counts" is now literal.

## Tools built during the session

`tools/hitbox-tester.html` (greyscale, red truth circles only, gate/hover/rain
scenarios, 1px precision mode, 6× magnifier) — built because "the sandbox is
too random; I need to verify the feel and see how close bullets can get."
The tester is what converted the hitbox argument from feelings to pixels.

## Coding

Flags 1–3: object = motion/audio polish, experience Understand/Confused →
resolved. Flags 4–5: object = display honesty; experience In-Control ladder —
the session's through-line is Satisfied only when visuals and rules agree.
Boss art to match the families: logged, next art item (wiki §8.11 scope).

## Process notes

Session ran long (research mid-session, twice) — worked here because the game
changes were renderer-only; the midboss glide was the only referee-touching
fix and got its control run. Jacob's screenshot markup (Cmd+Shift+4 → thumbnail
→ Markup) entered the loop for the first time and settled the bullet-core
argument in one image.
